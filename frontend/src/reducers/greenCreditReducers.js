const initialState = {
    greenCredits: [],
  };
  
  export const greenCreditReducer = (state = initialState, action) => {
    switch (action.type) {
      // Define your green credit-related action handlers
      default:
        return state;
    }
  };
  