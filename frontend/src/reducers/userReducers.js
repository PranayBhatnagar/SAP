const initialState = {
    user: null,
  };
  
  export const userReducer = (state = initialState, action) => {
    switch (action.type) {
      // Define your user-related action handlers
      default:
        return state;
    }
  };
  